from CQs.cq04.concatenation import concat
from CQs.cq04.cordinates import get_coords  # Import get_coords from coordinates.py

# Global variables
x = "123"
y = "abc"

# Call concat using x and y as arguments and print the result
print(concat(x, y))

# Call get_coords using x and y as arguments
get_coords(x, y)
